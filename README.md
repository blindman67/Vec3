##  Vec3 Simple 3D vector class

Vec3 chainable performance orientated vector class for general purpose use, and can be used along-side THREE's `Vector3` and `Matrix4`

Import `Vec3` and alias `V3`. If using with THREE import `TV3` as alias to `THREE.Vector3`
